
#{SectionLabel} img { border:5px ridge grey;}
#{SectionLabel} .container {
  display: flex;
  border:3px solid yellow;
}

#{SectionLabel} img {
  max-width: 90%
}

#{SectionLabel} .image {
  flex-basis: 70%
}

#{SectionLabel} .text {
  font-size: 20px;
  padding-left: 20px;
}