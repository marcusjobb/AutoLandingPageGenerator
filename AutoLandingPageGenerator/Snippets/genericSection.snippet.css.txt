#{SectionLabel}
{
  margin:{Margin};
  padding:{Padding};
  background-color:{BackgroundColor};
  font-size:{FontSize};
  color:{ForeColor};
  width:75%;
  height:100%;
  table-layout: fixed;
}
#{SectionLabel} H1 { text-align:center;}
#{SectionLabel} H2 { text-align:center;}
#{SectionLabel} H3 { text-align:center;}
#{SectionLabel} H4 { text-align:center;}

#{SectionLabel}bottomNav a, a:link a:visited a:hover a:active { color:{ForeColor};font-weight: bold; font-decoration:none;}

#{SectionLabel}bottomNav
{
  background-color:{BackgroundColor};
  font-size:{FontSize};
  color:{ForeColor};
}

#{SectionLabel}Container
{
  margin:0px;
  padding:0px;	
  background-color:{BackgroundColor};
  width:{Width};
  height:{Height};
}

#{SectionLabel}box {
  display: flex;
  flex-flow: column;
  height: 80%;
  background-color:{BackgroundColor};
  font-size:{FontSize};
  color:{ForeColor};
}
#{SectionLabel}box .row {
  background-color:{BackgroundColor};
  font-size:{FontSize};
  color:{ForeColor};
}
#{SectionLabel}box .row.header {
  flex: 0 1 auto;
  background-color:{BackgroundColor};
  font-size:{FontSize};
  color:{ForeColor};
}
#{SectionLabel}box .row.content {
  flex: 1 1 auto;
  background-color:{BackgroundColor};
  font-size:{FontSize};
  color:{ForeColor};
}
#{SectionLabel}box .row.footer {
  flex: 0 1 40px;
  background-color:{BackgroundColor};
  font-size:{FontSize};
  color:{ForeColor};
  font-weight: bold;
}
